[package]
name = "qflink-contracts"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
parity-scale-codec = { version = "3.6", default-features = false }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
